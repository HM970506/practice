#include <iostream>
#include <string>
#include <unordered_map>
using namespace std;

typedef int mincoding; //int라는 타입을 mincoding으로 바꿔줌

int main()
{
	unordered_multimap < string, int >m2 = { {"ABC",35},{"BBQ",44} };

	//undoreder_map은 m2["BBA"]=15로 추가되지만 multmap은 덮어쓰기됨.
	//insert로 추가해주어야 함

	m2.insert({ "BBQ",77 });


	return 0;
}

1. Header : #include <unordered_map>
2. 선언 : unordered_map<char, int> dic;
3. 사용 : dic['A']++;
4. 노드 존재 여부 확인 : if (dic.count('A') > 0)


https://https://BBQ.com.com.com.org


https://ABC.co.kr
https://.com
