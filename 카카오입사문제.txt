2020 카카오 블라인드채용

1번문제
1. 문자열 입력(string)
2. 문자열 글자수를 min개로 둠
3.n개 단위로 자르는 함수 (n개의 개수는 절반보다 작게)

 x+n로 이동
n개만큼 비교스트링에 담음
같으면 cnt++하고 지우기
같지 않고 cnt!=0이면 string result에 +cnt +비교스트링
같지 않은데 cnt==0이면 result에 +비교스트링
비교스트링 교체하고 반복

4.함수가 끝날 때마다 result 스트링 개수를 min값에 업데이트


2번문제
1.문자열 입력, 비었으면 빈 문자열 retrun
2.문자열이 비지 않았을 때 u와 v로 분리
u는 (와 )의 수를 각각 cnt해 같은 시점에서 u로 분리
v는 그 나머지부분

3.u가 올바른 괄호 문자열인지 검사
u를 백업해둠
처음이 )면 올바르지 않은 괄호 문자열
처음이 (면 )를 찾아 ()를 백업에서 제외, )가 없으면 올바르지 않은것
스트링이 빌때까지 반복

4.올바르다면 v로 2반복, u에 붙여 반환

5.올바르지 않다면
앞뒤 문자 삭제 후 )는 (로, (는 )로 바꿈
앞에 (, 뒤에 )붙여 리턴


3번문제
1.키와 자물쇠 각각 입력(bfs로 풀기)
2.키와 자물쇠가 맞는지 확인하는 함수
2차 포문을 돌며 키 부분을 0일시 자물쇠가 1이 아니면 cout faiil return0
키부분이 1인데 자물쇠가 0이 아니어도 같음
조건에 모두 해당한 경우 cout true

3.키를 회전하는 함수
최대 4번까지 회전
백업에 키 회전한 맵을 백업하고 백업을 키로 되돌림

4.키를 상하좌우로 이동하는 함수

