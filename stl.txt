#include <iostream>
#include <vector>
using namespace std;

int main()
{
	vector<pair <char, char>> a = { {'a','t'},{'b','c'} ,{'k','r'},{'g','d'} };

	for (int i = 0; i < a.size(); i++)
	{
		cout << a[i].first << a[i].second;
	}


	return 0;
}